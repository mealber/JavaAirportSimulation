package observer;

public class RoundObserver implements Observer {
    Simulation s;

    public RoundObserver(Simulation s) {
        this.s = s;
    }

    @Override
    public void update() {

    }
}